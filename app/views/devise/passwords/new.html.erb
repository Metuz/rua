<div class="login">
  <div class="logo">
		R<strong>UA</strong>A
	</div>
  <div class="box">
    <div class="content">
      <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, :class => "form-vertical login-form" }) do |f| %>
        <%= devise_error_messages! %>
        <h3 class="form-title">Olvidaste tu contraseña?</h3>
        <div class="form-group">
					<div class="input-icon">
						<i class="fa fa-envelope"></i>
            <%= f.email_field :email, autofocus: true, :class => "form-control", :placeholder => "Correo", :require => true %>
					</div>
				</div>
        <div class="form-actions">
          <%= f.submit "Aceptar", :class => "submit btn btn-primary pull-right" %>
				</div>
      <% end %>
    </div>
    <div class="inner-box">
      <%= render "devise/shared/links" %>
    </div>
</div>

<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @master.name %>
</p>

<p>
  <strong>Subjects:</strong>
  <% @master.subjects.each do |subject| %>
    <%= subject.name %>
  <% end %>
</p>

<p>
  <strong>Students:</strong>
  <% @master.students.each do |student| %>
    <%= student.pin %>
  <% end %>
</p>


<%= link_to 'Edit', edit_master_path(@master) %> |
<%= link_to 'Back', masters_path %>
